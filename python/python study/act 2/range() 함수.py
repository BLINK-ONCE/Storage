# range(n) # 0부터 n-1까지 range 자료형 반환
# range(m, n) # m부터 n-1까지 range 자료형 반환
# range(m, n, s) # m부터 시작하여 s만큼씩 증가시키면서 n-1까지 range자료형 반환

# e.g. of range() function
print(list(range(5)))
print(list(range(5, 10)))
print(list(range(1, 10, 2)))
